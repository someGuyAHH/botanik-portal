Options -Indexes
RewriteEngine On

# Serve real files and directories directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# All other requests → React SPA fallback
RewriteRule ^ index.html [L]
